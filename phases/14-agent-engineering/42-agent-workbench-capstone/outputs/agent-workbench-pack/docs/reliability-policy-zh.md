# 可靠性策略

该工作台吸收五种行业常发失败模式：

1. 幻觉动作（Hallucinated action）——由规则集 + 验证门控捕获。
2. 范围蔓延（Scope creep）——由范围合约 diff 检查捕获。
3. 级联错误（Cascading errors）——由反馈记录 + 拒绝空退出码捕获。
4. 上下文丢失（Context loss）——由仓库记忆吸收；聊天不是真相源。
5. 工具误用（Tool misuse）——由审查评分标准的验证维度捕获。

该策略由验证门控强制执行。覆盖路径经过签名和审计；智能体不能自我覆盖。