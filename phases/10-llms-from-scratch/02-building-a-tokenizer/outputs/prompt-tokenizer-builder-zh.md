---
name: prompt-tokenizer-builder
description: 为 LLM 项目构建和调试生产级分词器
version: 1.0.0
phase: 10
lesson: 2
tags: [tokenizer, bpe, byte-level, special-tokens, chat-template, multilingual]
---

# 生产级分词器构建器

在为 LLM 项目构建或调试分词器时，遵循此框架。

## 流水线检查清单

每个生产级分词器都需要以下五个阶段。如果缺少任何一个，生产环境中就会遇到边界问题。

1. **标准化（Normalize）** -- 应用 NFKC Unicode 标准化。这会折叠连字（"fi" -> "fi"）、标准化全角字符、标准化空白。跳过此步，同一单词会因输入方式不同而获得不同的 token ID。

2. **预分词（Pre-Tokenize）** -- 在 BPE 之前将文本分割为片段。英语中心模型使用 GPT-2 的正则模式。多语言模型使用 SentencePiece 的原始字节方案。此选择决定 BPE 是否可以跨词边界合并。

3. **BPE 合并（BPE Merge）** -- 在每个片段内对字节序列应用学习到的合并表。合并表就是分词器学习到的知识。其余一切都是辅助设施。

4. **特殊 Token 注入（Special Token Injection）** -- 在 BPE 运行前精确匹配特殊 token。[BOS]、[EOS]、[PAD]、聊天模板标记获得固定 ID。它们从不参与合并。

5. **ID 映射（ID Mapping）** -- 将 token 字符串转换为整数。模型仅看到整数。

## 诊断分词器问题

**症状：模型在聊天输入上产生乱码**
- 检查聊天模板。每个模型有不同的格式。Llama 3 使用 `<|start_header_id|>` 标记。ChatGPT 使用 `<|im_start|>` 标记。错误的模板会将输入置于训练分布之外。

**症状：非英语文本消耗过多 token**
- 检查繁殖率（每词 token 数）。高于 2.0 意味着分词器在该语言上浪费上下文窗口。解决方案：使用更多多语言数据重新训练、增加词汇量、或使用 SentencePiece 的 Unigram。

**症状：数字和算术失败**
- 检查数字如何被分词。"1234"作为一个 token 意味着模型无法做数字级操作。在预分词时单独拆分每个数字。

**症状：代码 token 效率低**
- 检查缩进如何处理。GPT-2 的分词器在空格上浪费 token。Codex 和 StarCoder 使用特殊缩进 token（4 空格 = 1 个 token）。

## 词汇量大小决策

- 32K token：单语言、小型模型、有限计算。嵌入层参数量为 32K × d_model。
- 50K-64K：多语言或代码密集型。大多数项目的良好平衡点。
- 100K+（GPT-4, Llama 3）：仅在大规模训练数据时使用。序列更短，但嵌入参数为 100K × d_model。

对于 4096 维模型：32K 词汇表 = 131M 嵌入参数。128K 词汇表 = 524M 嵌入参数。仅嵌入层就有 400M 参数。

## 速度要求

- 训练数据分词：使用 Rust 后端库（tiktoken、HuggingFace tokenizers）。纯 Python 慢 10-100 倍。
- 推理分词：延迟不太重要（单序列），但仍使用编译实现。
- 基准测试：对 1GB 文本分词并测量墙钟时间。如果超过 60 秒，切换到 Rust 后端。

## 聊天模板验证

部署任何聊天模型前，验证模板：

1. 用分词器编码已知对话
2. 解码回文本
3. 逐字符与模型文档中的预期格式对比
4. 注意：头 token 后的换行、内容前的空格、轮次结束标记
5. 测试边界情况：空系统消息、超长用户消息、多次助手回复

聊天模板错误是聊天模型性能下降的最常见原因。