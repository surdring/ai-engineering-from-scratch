---
name: prompt-sft-data-curator
description: 设计和策划用于监督微调（SFT）的指令数据集
version: 1.0.0
phase: 10
lesson: 6
tags: [sft, instruction-tuning, fine-tuning, data-curation, alignment]
---

# SFT 数据策划器

在为一个特定能力（代码生成、数学、对话、安全）设计指令调优数据集时，使用此框架规划数据收集、定义质量标准并构建训练流水线。

## 输入要求

提供：
- **目标能力**（例如"Python 代码生成"、"医疗问答"、"多轮对话"）
- **基础模型**（例如 Llama 3 8B、Mistral 7B、Qwen 2.5 72B）
- **预算**（标注工时、合成生成的 API 成本）
- **格式偏好**（Alpaca、ShareGPT、ChatML）

## 第一步：数据集设计

### 数据量指南

| 质量级别 | 所需示例 | 预期结果 |
|--------------|----------------|------------------|
| 研究原型 | 1,000-5,000 | LIMA 级别：如果示例是专家编写的，与更大数据集效果相当 |
| 生产 v1 | 10,000-50,000 | Stanford Alpaca 级别：在常见任务上具有扎实的指令遵从能力 |
| 生产 v2 | 50,000-200,000 | Vicuna/Llama 2 Chat 级别：稳健的多轮对话，领域覆盖 |

质量始终胜过数量。1,000 个专家编写的示例（LIMA，2023 年 5 月）与在 50,000+ 示例上训练的模型效果相当。优先级：

1. **多样性** -- 覆盖目标能力的全部范围
2. **准确性** -- 每个回答必须事实正确
3. **清晰度** -- 回答应简洁且结构良好
4. **难度梯度** -- 包含简单、中等和困难示例

### 多样性检查清单

对于通用助手：
- 开放式问题（20%）
- 事实问答（20%）
- 创意写作（10%）
- 代码生成（15%）
- 推理和数学（15%）
- 摘要（10%）
- 带约束的指令遵从（10%）

针对领域特定模型调整百分比。编码助手可将 60% 分配给代码生成，20% 分配给代码解释。

## 第二步：数据格式

### Alpaca 格式（单轮）

```json
{
  "instruction": "写一个在 Python 中反转字符串的函数。",
  "input": "",
  "output": "def reverse_string(s):\n    return s[::-1]"
}
```

适用场景：单轮任务、简单指令-回答对、快速原型。

### ShareGPT 格式（多轮）

```json
{
  "conversations": [
    {"from": "system", "value": "你是一名 Python 专家。"},
    {"from": "human", "value": "如何反转一个字符串？"},
    {"from": "gpt", "value": "使用切片：s[::-1]"},
    {"from": "human", "value": "那列表呢？"},
    {"from": "gpt", "value": "同样的语法：my_list[::-1]"}
  ]
}
```

适用场景：对话应用、多轮上下文很重要时。

### ChatML 格式（含特殊 token）

```
<|im_start|>system
你是一名 Python 专家。<|im_end|>
<|im_start|>user
如何反转一个字符串？<|im_end|>
<|im_start|>assistant
使用切片：s[::-1]<|im_end|>
```

适用场景：面向原生使用 ChatML 的模型（Qwen、Yi）。

## 第三步：质量标准

### 每示例检查

1. **回答相关性**：回答是否确实针对指令？
2. **事实准确性**：所有声明是否可验证且正确？
3. **完整性**：回答是否完全回应指令？
4. **简洁性**：是否能用更少词语传达同样信息？
5. **格式一致性**：回答是否符合预期风格？

### 危险信号（拒绝该示例）

- 回答内部自相矛盾
- 回答包含有害内容且未拒绝
- 回答产生幻觉的事实或引用
- 指令模糊且回答未澄清
- 回答是重新措辞的指令副本

### 数据集级别检查

- 单个来源/模板的示例不超过 5%
- 至少 80% 的回答 token 是有意义的（非填充内容）
- 平均回答长度为 50-200 token（避免过短或过长）
- 系统提示多样性：至少涵盖 10 种不同的系统提示

## 第四步：训练配置

| 参数 | 推荐范围 | 备注 |
|-----------|------------------|-------|
| 学习率 | 1e-5 到 5e-5 | 更大模型用更低值（70B 用 1e-5，7B 用 5e-5） |
| 轮数 | 1-3 | 监控验证损失，一旦上升就停止 |
| 批次大小 | 32-128 | 如 GPU 受限则用梯度累积放大 |
| 预热 | 0-5% 步数 | 不如预训练关键 |
| 权重衰减 | 0.0-0.1 | 短时微调可选 |
| 损失掩码 | 仅回答 token | 掩码掉指令和系统提示 token |
| 预训练数据混合 | 2-5% | 混合原始文本防止灾难性遗忘 |

## 第五步：评估协议

训练后评估：

1. **指令遵从率**：测试提示中模型产生相关且完整回答的百分比
2. **遗忘分数**：对比基础模型在留出通用文本语料上的困惑度
3. **格式合规性**：遵循预期聊天格式的回答百分比
4. **MT-Bench 或 AlpacaEval**：指令调优模型的标准基准
5. **领域特定评估**：针对目标能力的自定义评估

### 警告信号

- 第 1 轮后验证损失上升：过拟合，减少轮数或增加数据
- 遗忘分数增加 > 15%：学习率过高或轮数过多
- 模型逐字复现训练示例：严重过拟合，需要更多样化的数据
- 模型拒绝良性指令：过度训练于安全数据，重新平衡数据集