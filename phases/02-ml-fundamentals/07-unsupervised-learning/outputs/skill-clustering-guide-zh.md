---
name: skill-clustering-guide
description: 根据聚类形状、噪声和约束条件选择合适的聚类算法
version: 1.0.0
phase: 2
lesson: 7
tags: [聚类, k-means, dbscan, 层次聚类, gmm, 无监督]
---

# 聚类算法选择指南

聚类没有唯一的最佳算法。正确的选择取决于聚类形状、是否知道聚类数量、数据中的噪声量以及数据集规模。

## 决策清单

1. 是否知道聚类数量？
   - 是：K-Means 或 GMM
   - 否：DBSCAN（自动发现聚类），或层次聚类（在不同层次截断树状图）

2. 聚类是什么形状？
   - 大致球形（团状）：K-Means
   - 不同大小的椭圆形：GMM
   - 任意形状（月牙形、环形、链状）：DBSCAN
   - 嵌套或层次结构：层次聚类

3. 数据是否包含噪声或离群值？
   - 是：DBSCAN（显式标记噪声点）或 GMM（低概率点为离群值）
   - 否：K-Means 就可以

4. 是否需要软分配（概率）？
   - 是：GMM 给出每个聚类的 P(cluster | data point)
   - 否：K-Means 或 DBSCAN 给出硬分配

5. 数据集有多大？
   - 10,000 以下：任何算法都可以
   - 10,000 到 1,000,000：K-Means（快），Mini-Batch K-Means（更快）
   - 1,000,000 以上：Mini-Batch K-Means 或 BIRCH。层次聚类太慢。

## 各方法适用场景

**K-Means**：默认起点。快（O(n * k * iterations)），简单，对许多问题来说足够好。使用肘部法（Elbow Method）或轮廓系数选择 K。局限性：假设球形聚类，对初始化敏感（使用 K-Means++ 或多次运行），不能很好地处理不同大小的聚类。

**DBSCAN**：最适合发现任意形状的聚类和自动检测离群值。两个参数：eps（邻域半径）和 min_samples（最小密度）。不需要指定 K。局限性：当聚类密度差异很大时表现不佳，调优 eps 可能很难。使用 k-距离图估计 eps：计算每个点到第 k 近邻的距离，排序后寻找拐点。

**层次聚类（聚合式）**：构建合并树。适用于希望在多个粒度级别探索聚类结构（在不同高度截断树状图）的场景。Ward 链接对紧凑聚类效果最好。单链接可找到伸长聚类但对噪声敏感。局限性：O(n^2) 内存和 O(n^3) 时间，对大数据集不实用。

**GMM（高斯混合模型）**：带概率分配的软聚类。将每个聚类建模为具有独立均值和协方差的高斯分布。当聚类为椭圆形或重叠时比 K-Means 更好。使用 BIC（贝叶斯信息准则）选择成分数量。局限性：假设高斯分布，在非凸形状上可能失败，对初始化敏感。

## 评估聚类质量（无标签）

| 指标 | 衡量内容 | 范围 | 适用场景 |
|------|---------|------|--------|
| 轮廓系数（Silhouette） | 凝聚度 vs 分离度 | -1 到 1（越高越好） | 比较 K 值或算法 |
| 惯性（簇内平方和） | 聚类紧密度 | 0 到 inf（越低越好） | K-Means 的肘部法 |
| BIC / AIC | 模型拟合度加复杂度惩罚 | 越低越好 | 选择 GMM 成分数量 |
| Calinski-Harabasz 指数 | 簇间方差与簇内方差之比 | 越高越好 | 快速比较 |
| Davies-Bouldin 指数 | 聚类间的平均相似度 | 越低越好 | 惩罚重叠聚类 |

## 常见错误

- 不缩放特征就运行 K-Means（较大尺度的特征主导距离计算）
- 在 2D 中目测数据选择 K，而实际数据是高维的（使用轮廓系数）
- 在非球形聚类上使用 K-Means（月牙形或环形数据需要 DBSCAN）
- DBSCAN 的 eps 设置太大（所有点在一个聚类中）或太小（所有点都是噪声）
- 将聚类标签当作真实标签（聚类是探索性的，需用领域知识验证）
- 在超过 20,000 个点的数据集上运行层次聚类（内存和时间爆炸）

## 快速参考

| 算法 | 聚类形状 | 自动发现 K | 处理噪声 | 软分配 | 可扩展性 |
|------|---------|-----------|---------|--------|---------|
| K-Means | 球形 | 否（由你设置 K） | 否 | 否 | 百万级 |
| Mini-Batch K-Means | 球形 | 否 | 否 | 否 | 千万级 |
| DBSCAN | 任意 | 是 | 是 | 否 | 数十万级 |
| 层次聚类 | 任意（取决于链接方式） | 灵活（截断树状图） | 取决于链接方式 | 否 | 2 万以下 |
| GMM | 椭圆形 | 否（由你设置 K） | 部分（低概率） | 是 | 10 万以下 |
| HDBSCAN | 任意 | 是 | 是 | 部分 | 数十万级 |