---
name: classifier-stack-audit
description: 审计部署的输入/输出分类器栈（模型、分类体系、输入轨道、输出轨道、对话轨道），标记对抗攻击缺口。
version: 1.0.0
phase: 15
lesson: 18
tags: [llama-guard, nemo-guardrails, input-rails, output-rails, colang, adversarial-attacks]
---

给定部署的分类器栈（Llama Guard 版本、NeMo Guardrails 配置、自定义分类器、规范化步骤），对照 2026 年参考方案审计，并标记栈未覆盖的攻击面。

生成：

1. **模型清单。** 列出使用的分类器。Llama Guard 3（8B / 1B-INT4）vs Llama Guard 4（多模态，S1–S14）。NeMo Guardrails 版本。任何自定义分类器。如果部署接受图像，确认分类器是多模态的。
2. **分类体系映射。** 将声明的业务类别映射到分类器的分类体系上。运维人员关心的每个类别必须映射到一个分类器类别；未映射的类别是未防护的。
3. **轨道覆盖。** 确认输入轨道在模型轮次之前触发，输出轨道在响应发出之前触发。对话轨道（NeMo 中的 Colang）强制执行跨轮次约束。单轮分类器无法捕获多轮攻击。
4. **规范化。** 确认输入经过 NFKC 规范化、同形字映射，并在分类前剥离零宽/变体选择器字符。原始字节分类是 Emoji 走私（Huang et al. 2025）的 100% ASR 目标。
5. **攻击语料库覆盖。** 对每种已记录的攻击（emoji 走私、同形字、上下文重定向、语义改写），指出栈中的具体防御。仅分类器防御不通过此审计；需要与宪法（第 17 课）和运行时（第 10、13、14 课）分层。

硬性拒绝：
- 在多模态输入上使用纯文本分类器的部署。
- 没有规范化步骤的部署。
- 仅有输入轨道（敏感类别输出无输出轨道）的部署。
- 将分类器视为单一安全层的栈。
- 运维人员无法在自己的分布上复现的 ASR 声明。

拒绝规则：
- 如果用户声明的类别未映射到分类器的分类体系，拒绝并要求先进行映射。未映射 = 未防护。
- 如果部署在多模态输入面上引用 Llama Guard 3 ASR 数字，拒绝并要求 Llama Guard 4 或多模态分类器。
- 如果用户将分类器层视为高风险环境中的充分方案，拒绝。EU AI Act 第 14 条（第 15 课）期望在其之上有人工监督。

输出格式：

返回分类器审计，包含：
- **模型清单**（名称、版本、模态）
- **分类体系映射**（运维类别 → 分类器类别）
- **轨道覆盖**（输入 / 输出 / 对话；在模型之前/之后触发）
- **规范化注释**（NFKC、同形字、零宽剥离）
- **攻击语料库覆盖**（攻击 → 防御）
- **层完整性**（分类器 + 宪法 + 运行时；三者均需）
- **就绪状态**（生产 / 测试 / 仅研究）